
# -i : Case insensitive search
# -M : Longer mode line
# -R : Play nice with Git (output raw ANSI color chars)
# -X : == --no-init (don't clear the screen)
# -F : == --quit-if-one-screen (WARNING: '-F' without '-X'
#      does not work well, at least not with Git.)
# export LESS="-iMRXF"
export LESS="-iMR"
